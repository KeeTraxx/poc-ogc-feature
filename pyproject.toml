[project]
name = "poc-ogc-feature"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pygeoapi",
    "tinydb",
    "opensearch-py",
    "opensearch-dsl",
    "packaging>=26.0",
]

[tool.uv]
dev-dependencies = []
